# Bungeecord-Expansion Fork
This is a fork of the original expansion for bungeecord placeholders but that takes into account the fake players from UltraSpoof to not use their connection to retrieve datas from the proxy.

You will have to drag the JAR into `plugins/PlaceholderAPI/expansions/`
